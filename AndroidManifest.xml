<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="jupiter.broadcasting.live.holo"
          android:versionCode="11"
          android:versionName="3.1">

    <uses-sdk
            android:minSdkVersion="9"
            android:targetSdkVersion="19"/>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
            android:icon="@drawable/jb_icon1"
            android:label="@string/app_name"
            android:allowBackup="true"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
            >
        <activity
                android:name=".Home"
                android:label="@string/app_name"
                android:launchMode="singleTask"
                android:configChanges="keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <receiver
                android:name=".Home$NotificationReceiver">
            <intent-filter>
                <action android:name="DO_NOTIFICATION_ACTION"/>
            </intent-filter>
        </receiver>
        <activity android:name=".ShowActivity"
            android:configChanges="orientation|screenSize|keyboardHidden">
        </activity>
        <activity android:name=".JBPlayer"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden">
        </activity>
        <receiver
            android:name=".JBPlayer$NotificationReceiver">
            <intent-filter>
                <action android:name="DO_NOTIFICATION_ACTION"/>
            </intent-filter>
        </receiver>
        <activity android:name=".Catalogue"
            android:configChanges="orientation|screenSize|keyboardHidden">
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".Home" />

        </activity>
    </application>
</manifest>